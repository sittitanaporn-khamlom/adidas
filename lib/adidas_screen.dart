import 'package:flutter/material.dart';

class AdidasScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Welcome to Adidas Home", style: TextStyle(fontSize: 20)),
    );
  }
}
